pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.PREFER_SETTINGS)  // ⬅️ КРИТИЧЕСКИ ВАЖНО
    repositories {
        google()
        mavenCentral()
    }
}
rootProject.name = "STunnel"
include ':app'
